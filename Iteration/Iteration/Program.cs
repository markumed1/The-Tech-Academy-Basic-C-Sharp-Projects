using System;
using System.Collections.Generic;


namespace Iteration
{
    class Program
    {
        static void Main(string[] args)
        {
            //int[] testScores = { 98, 99, 85, 70, 82, 34, 90, 94 };

            //// The for loop executes a block of statements repeadedly until the
            ////specified condition returns false.
            //for (int i = 0; i < testScores.Length; i++) 
            //{
            //    if (testScores[i] > 85)
            //    {
            //        Console.WriteLine("Passing test score: " + testScores[i]);
            //    }
            //}
            //Console.ReadLine();

            string[] names = { "Jese", "Erik", "Daniel", "Adam" };

            for (int j = 0; j < names.Length; j++)
            {
                if (names(j))
            }
        }
    }
}
